/**
 * This file defines the top-level React component for the Chapterly UI.
 * For now it only proves the frontend toolchain works; features come later.
 */

export function App() {
  return (
    <main className="app-shell">
      <h1>Chapterly</h1>
      <p>Boilerplate frontend is running.</p>
    </main>
  );
}
