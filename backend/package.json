{
  "name": "chapterly-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chapterly API server (Express + PostgreSQL in later steps).",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
