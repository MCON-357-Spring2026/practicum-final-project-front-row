# Port the API listens on (Render sets PORT automatically in production).
PORT=4000

# PostgreSQL connection string (used when we add database code in a later step).
# Example for local development:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/chapterly
